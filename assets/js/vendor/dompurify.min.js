/*!
 * DOMPurify - ARCHIVO DE VENDOR LOCAL (FALLBACK)
 *
 * INSTRUCCIONES DE INSTALACIÓN:
 * ==============================
 * Este archivo NO se genera automáticamente. Debe obtenerse manualmente
 * desde el repositorio oficial de DOMPurify en GitHub.
 *
 * PASO 1 — Descargar el archivo oficial:
 *   URL: https://raw.githubusercontent.com/cure53/DOMPurify/main/dist/purify.min.js
 *
 * PASO 2 — Guardar en esta ruta exacta:
 *   assets/js/vendor/dompurify.min.js
 *
 * PASO 3 — Calcular hash SRI local (para verificación):
 *   cat assets/js/vendor/dompurify.min.js | openssl dgst -sha384 -binary | openssl base64 -A
 *
 * PASO 4 — Verificar tags <script> en index.html, historias.html y noticias.html:
 *
 *   <!-- CDN principal con SRI -->
 *   <script
 *     src="https://cdn.jsdelivr.net/npm/dompurify@3/dist/purify.min.js"
 *     integrity="sha384-HASH_OFICIAL_DE_JSDELIVR"
 *     crossorigin="anonymous"
 *     defer>
 *   </script>
 *
 *   <!-- Fallback local -->
 *   <script>
 *     window.DOMPurify || document.write(
 *       '<script src="./assets/js/vendor/dompurify.min.js" defer><\/script>'
 *     );
 *   </script>
 *
 * PASO 5 — Verificar que search.js consume window.DOMPurify sin import:
 *   const clean = window.DOMPurify.sanitize(userInput);
 *
 * ADVERTENCIA:
 * ============
 * NO escribas código propio en este archivo.
 * NO modifiques el contenido descargado.
 * NO renombres este archivo.
 * NO crees archivos adicionales en assets/js/vendor/.
 *
 * Si el archivo ya contiene el código minificado de DOMPurify,
 * este paso está completo. No hay nada más que hacer.
 *
 * Fuente oficial: https://github.com/cure53/DOMPurify
 * Licencia: Apache-2.0 / Mozilla Public License 2.0
 */